<section class="hero">
  <div class="page-width hero__grid">
    <div class="hero__content">
      {%- if section.settings.eyebrow != blank -%}<p class="eyebrow">{{ section.settings.eyebrow }}</p>{%- endif -%}
      <h1 class="hero__title">{{ section.settings.heading }}</h1>

      {%- if section.settings.show_rating -%}
        <div class="hero__rating">
          <span class="stars">★★★★★</span>
          <span>{{ section.settings.rating_text }}</span>
        </div>
      {%- endif -%}

      <p class="lede">{{ section.settings.subheading }}</p>

      <div class="hero__cta">
        {%- if section.settings.button_label != blank -%}
          <a href="{{ section.settings.button_link | default: routes.all_products_collection_url }}" class="btn btn--accent btn--lg">{{ section.settings.button_label }}</a>
        {%- endif -%}
        {%- if section.settings.button_label_2 != blank -%}
          <a href="{{ section.settings.button_link_2 }}" class="btn btn--lg">{{ section.settings.button_label_2 }}</a>
        {%- endif -%}
      </div>

      <ul class="hero__perks">
        <li>✓ Free SA shipping</li>
        <li>✓ 30-day money-back</li>
        <li>✓ Tracked delivery</li>
      </ul>
    </div>

    <div class="hero__media">
      {%- if section.settings.image != blank -%}
        <img class="hero__img" src="{{ section.settings.image | image_url: width: 1000 }}" alt="{{ section.settings.heading | escape }}" width="1000" height="1250" loading="eager">
      {%- else -%}
        <div class="hero__placeholder">
          <span class="hero__placeholder-emoji">✨</span>
          <span class="hero__placeholder-text">Your product photo goes here<br><small>Customize → Hero → Hero image</small></span>
        </div>
      {%- endif -%}
      {%- if section.settings.badge != blank -%}<span class="hero__badge">{{ section.settings.badge }}</span>{%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Hero",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Microcurrent · EMS · 7-colour LED" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Your 5-minute facelift." },
    { "type": "textarea", "id": "subheading", "label": "Subheading", "default": "Lúma combines microcurrent, EMS and 7-colour LED light therapy to lift, sculpt and firm — a snatched jawline, a defined neck and brighter skin. Salon tech, on your bathroom counter." },
    { "type": "text", "id": "button_label", "label": "Button label", "default": "Shop the Sculptor" },
    { "type": "url", "id": "button_link", "label": "Button link" },
    { "type": "text", "id": "button_label_2", "label": "Secondary button label", "default": "How it works" },
    { "type": "url", "id": "button_link_2", "label": "Secondary button link", "default": "#how-to" },
    { "type": "image_picker", "id": "image", "label": "Hero image" },
    { "type": "text", "id": "badge", "label": "Image badge", "default": "Loved on TikTok" },
    { "type": "checkbox", "id": "show_rating", "label": "Show rating", "default": true },
    { "type": "text", "id": "rating_text", "label": "Rating text", "default": "Join 8,000+ snatched faces" }
  ],
  "presets": [{ "name": "Hero" }]
}
{% endschema %}
